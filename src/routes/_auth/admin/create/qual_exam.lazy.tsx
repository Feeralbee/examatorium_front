import { createLazyFileRoute } from "@tanstack/react-router";

const CreateQualExamPage = () => {
  return;
};

export const Route = createLazyFileRoute("/_auth/admin/create/qual_exam")({
  component: CreateQualExamPage,
});
